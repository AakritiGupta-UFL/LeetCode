class Solution {
    public int numJewelsInStones(String J, String S) {
        int result=0;
        for(char c:J.toCharArray())
        {
            for(char d:S.toCharArray())
            {
                if(c==d)
                    result++;
            }
        }
        return result;
    }
}
